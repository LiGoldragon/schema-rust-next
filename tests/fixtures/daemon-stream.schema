{}
[Record (SubscribeIntent SubscribeIntent opens IntentEventStream)]
[RecordAccepted SubscriptionStarted (Event IntentEvent)]
{
  Topic String
  Topics (Vector Topic)
  Description String
  RecordIdentifier Integer
  SubscriptionToken Integer

  Record Entry
  SubscribeIntent Query

  RecordAccepted RecordIdentifier
  SubscriptionStarted IntentSubscription

  Entry { Topics * Description * }
  Query { Topics * }
  IntentSubscription { SubscriptionToken * }
  IntentRecorded { Entry * RecordIdentifier * }
  IntentEvent [(IntentRecorded IntentRecorded belongs IntentEventStream)]
  IntentEventStream (Stream { token SubscriptionToken opened SubscriptionStarted event IntentEvent close SubscriptionToken })
}
