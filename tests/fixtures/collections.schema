((Register Cluster) (Observe Query))
((Projected (Map (NodeName NodeConfig))) (Listed (Vec NodeName)))
{
  NodeName [Text]
  NodeConfig [Text]
  Service [Text]
  Query [Topic]
  Topic [Text]
  Cluster [(services (Vec Service)) (nodes (Map (NodeName NodeConfig))) (cache (Optional NodeConfig))]
}
