[package]
name         = "schema-rust-next"
version      = "0.1.0"
edition      = "2024"
rust-version = "1.85"
license      = "MIT OR Apache-2.0"
repository   = "https://github.com/LiGoldragon/schema-rust-next"
description  = "Rust source emitter for schema-next assembled schemas."
publish      = false

[lib]
name = "schema_rust_next"
path = "src/lib.rs"

[dependencies]
schema-next = { git = "https://github.com/LiGoldragon/schema-next.git", branch = "main" }

[dev-dependencies]
rkyv = { version = "0.8", default-features = false, features = ["std", "bytecheck", "little_endian", "pointer_width_32", "unaligned"] }

[lints.rust]
unsafe_code = "forbid"
unused      = { level = "warn", priority = -1 }
dead_code   = "warn"
